<form method="post" action="{{ path('questions_delete', {'id': questionss.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ questionss.id) }}">
    <button class="btn">Delete</button>
</form>
